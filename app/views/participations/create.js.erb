if(!$("#claims_<%= @p.result.id %>:contains('<%= Lookup.find(@p.participation_type).description %>')").length) {
  $('#claims_<%= @p.result.id %>').html("<% participations(@p.result, current_user) %>");
}
