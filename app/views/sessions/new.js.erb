jQuery('#log-in-form')
    .html("<%= escape_javascript(render(partial: 'form')) %>");

jQuery('#log-in-form').modal();

validate('session-form', {
    'email': "required",
    'password': "required"
});